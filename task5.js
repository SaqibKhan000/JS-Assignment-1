



//TASK 5 //////////////////////////////////









                          ////////////////////// VALID VARIABLES ///////////////

                        // userName - (Valid)
                        // It starts with a letter, uses camelCase, and does not contain any restricted characters.


                        // $totalAmount - (Valid)
                        // Variable names can start with a $ and contain valid characters.


                        // _hiddenValue - (Valid)
                        // Variable names can start with an underscore _ and are commonly used for private or internal variables.


                        // firstName1 - (Valid)
                        // The variable name starts with a letter and contains only valid characters (letters, numbers, underscores).


                        // 2ndPlace - (Invalid)
                        // JavaScript variable names cannot start with a number.













                          ////////////////////// INVALID VARIABLES ///////////////


                            // class - (Invalid)
                            // class is a reserved keyword in JavaScript and cannot be used as a variable name.


                            // user-name - (Invalid)
                            // Hyphens - are not allowed in variable names; JavaScript interprets it as a minus operator.


                            // 123variable - (Invalid)
                            // Variable names cannot begin with a number, although they can contain numbers elsewhere.

                            // var - (Invalid)
                            // var is a reserved keyword in JavaScript and cannot be used as a variable name.

                            // my.variable - (Invalid)
                            // The dot . is not allowed in variable names. It is used to access properties of objects.





                                                        












